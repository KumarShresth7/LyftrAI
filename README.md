# Lyftr AI Backend Assignment

A containerized webhook processing service built with FastAPI, SQLite, and Docker.

## How to Run

1. **Start the application:**
   ```bash
   make up
   # Or manually: docker compose up -d --build